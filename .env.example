SECRET_KEY=(Run commnad >> node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"  )
FLASK_ENV=development

# Database configuration
DATABASE_URL=sqlite:///kubecertify.db

# Email configuration (for alerts)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=usre@gmail.com
SMTP_PASSWORD=password